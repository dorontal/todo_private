import 'package:flutter/material.dart';

// This file contains constants that affect the style of the app in
// various widgets

final appTheme = ThemeData.dark();

/// Screen width below which the screen is single column, rather than
/// two-column, with a menu icon at the top left that opens up the
/// drawer.
const splitViewMaxWidth = 670;

// const buttonColor = Color.fromRGBO(239, 68, 35, 1.0);
// const buttonColor = Color(0xffef4423);

const logoImageURL =
    'https://wg9r5td5e.tracktunes.org/assets/image/tracktunes-logo.png';
